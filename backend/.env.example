# Environment Configuration

# Application Settings
APP_NAME=Quantitative Strategy & Risk Dashboard
APP_VERSION=0.1.0
DEBUG=True
ENVIRONMENT=development

# Server Settings
HOST=0.0.0.0
PORT=8000
RELOAD=True

# CORS Settings (comma-separated origins)
CORS_ORIGINS=http://localhost:3000,http://localhost:3001

# Redis Configuration
REDIS_HOST=localhost
REDIS_PORT=6379
REDIS_DB=0
REDIS_PASSWORD=
CACHE_TTL=3600

# Database Configuration
DATABASE_URL=postgresql://user:password@localhost:5432/quant_db
DB_ECHO=False

# JWT Authentication
SECRET_KEY=your-secret-key-change-this-in-production
ALGORITHM=HS256
ACCESS_TOKEN_EXPIRE_MINUTES=30

# Financial Data Settings
DEFAULT_START_DATE=2022-01-01
DEFAULT_LOOKBACK_DAYS=730
YFINANCE_TIMEOUT=30

# HRP Settings
DEFAULT_LINKAGE_METHOD=ward
MIN_CORRELATION_PERIODS=0.8
DEFAULT_P_VALUE_THRESHOLD=0.05

# Risk-Free Rate (as decimal, e.g., 0.05 for 5%)
RISK_FREE_RATE=0.05

# Logging
LOG_LEVEL=INFO
LOG_FORMAT=json
